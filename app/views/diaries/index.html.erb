<% @diary.each do |diary| %>
<tr>
  <td><%= diary.comment %></td>
  <td>
    <%= link_to diary_path(diary.id) do %>
      <p>投稿日：<%= diary.created_at.strftime('%Y/%m/%d') %></p>
    <% end %>
  </td>
</tr>
<% end %>