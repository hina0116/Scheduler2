<div class="container">
  <div class="row">
    <div class="col-lg-8">
      <p><%= @diary.comment %></p>
      <p></p>
      <p>作成日：<%= @diary.created_at.strftime('%Y/%m/%d') %></p>
        <%= link_to "削除",  diary_path(@diary), method: :delete, class: "btn btn-outline-danger" %>
      <p><%= link_to '一覧に戻る', diaries_path %></p>
    </div>
  </div>
</div>