<h1>メモ</h1>
<div>
  <p>タイトル：<%= @task.name %></p>
  <p>内容：<%= @task.content %></p>
  <p>作成日：<%= @task.created_at.strftime('%Y/%m/%d') %></p>
  <%= link_to "修正", edit_task_path(@task.id), class: "btn btn-outline-secondary" %>
  <p><%= link_to '一覧に戻る', tasks_path %></p>
</div>