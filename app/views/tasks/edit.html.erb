<h1>editing</h1>
<%= form_with model: @task, url: task_path(@task.id), method: :patch do |f| %>

  <h4>title</h4>
  <%= f.text_field :name %>

  <h4>text</h4>
  <%= f.text_area :content %>

  <%= f.submit '保存' %>

<% end %>
